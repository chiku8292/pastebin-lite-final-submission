# Pastebin Lite

Submission-ready Pastebin-like application.

## Features
- TTL expiration
- View-based expiration
- REST APIs
- HTML paste viewer
- Deterministic test mode
- Persistent storage (Redis)

## API Endpoints
- GET /api/healthz
- POST /api/pastes
- GET /api/pastes/:id
- GET /p/:id
